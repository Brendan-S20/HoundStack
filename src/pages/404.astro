---
import BaseLayout from '../layouts/BaseLayout.astro';

// MKT-6: noindex as a second line of defence. A static host is supposed to
// serve this body with a 404 status, which keeps it out of the index on its
// own — but a misconfigured rewrite that returns 200 turns it into a soft
// 404 that gets indexed and outranks nothing. The header costs nothing and
// does not depend on the host being configured correctly. It is also why
// this page has a canonical to itself rather than to /.
---

<BaseLayout
  title="Page not found | HoundStack"
  description="That page does not exist. Head back to the HoundStack home page, features, or pricing."
  noindex
>
  <section class="nf">
    <div class="container text-center">
      <span class="eyebrow">404</span>
      <h1>This yard is empty</h1>
      <p class="lede nf-lede">
        The page you were after does not exist or has moved. The useful stuff is one click away.
      </p>
      <div class="nf-links">
        <a href="/" class="btn btn-primary">Home</a>
        <a href="/features" class="btn btn-ghost">Features</a>
        <a href="/pricing" class="btn btn-ghost">Pricing</a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .nf {
    padding-block: clamp(80px, 12vw, 140px);
  }

  .nf-lede {
    margin-inline: auto;
  }

  .nf-links {
    display: flex;
    gap: 14px;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 24px;
  }
</style>
